using VS.Base.Popup;

public class Panel_Credit : Base_AnimationPopup
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
